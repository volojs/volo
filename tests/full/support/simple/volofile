/*jslint node: true */
'use strict';

module.exports = {
    foo: {
        run: function (d, v, namedArgs) {
console.log('version lib: ' + (typeof v.require));

console.log('version lib: ' + v.require('./lib/volo/version'));
            var version = v.require('./lib/volo/version'),
                text = typeof version.compare;

            console.log("HELLO: " + text);
            v.write('compareType.txt', text);
        }
    }
};
