
//Light it up!
requirejs(['volo/main'], function (main) {
    main(function (message) {
        if (message) {
            console.log(message);
        }
    }, function (err) {
        console.log('ERROR: ' + err);
    });
});
