
//Light it up!
requirejs(['pkg/main'], function (main) {
    main(function (message) {
        if (message) {
            console.log(message);
        }
    }, function (err) {
        console.log('ERROR: ' + err);
    });
});
